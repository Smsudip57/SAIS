import { baseApi } from "../baseApi";

export const userApi = baseApi.injectEndpoints({
    endpoints: (builder) => ({
        // Placeholder for future user-related endpoints
        // Current trading APIs are in tradingApi
    }),
    overrideExisting: false,
});
